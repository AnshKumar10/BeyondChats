
import React from "react";
import EmailLayout from "@/components/email/EmailLayout";
import EmailContainer from "@/components/email/layout/EmailContainer";

const EmailInterface: React.FC = () => {
  return (
    <EmailContainer>
      <EmailLayout />
    </EmailContainer>
  );
};

export default EmailInterface;
